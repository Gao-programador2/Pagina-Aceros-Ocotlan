# Copia a .env.docker y ajusta antes de: docker compose --env-file .env.docker up -d --build

WEB_PORT=8080

# Build del front (opcional; vacío = mismo dominio vía proxy /api)
VITE_GA_MEASUREMENT_ID=G-E0JZ8EXZ6V
VITE_RECAPTCHA_SITE_KEY=6LcN9GAtAAAAAHyFQ0h-beFCvzCN-hsHJ5iZBlHQ
